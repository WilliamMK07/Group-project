# Group-project

To run the file this can be done in the terminal using the python command. Using the terminal enter "python", then the name of the the desired file to run e.g. "event_planner.py", finally choose the desired text file to run using algorithms and make sure the correct file path is used. For example if the list of text files are stored in a file called "Input_Files" use file name "Lists/input_10.txt" for example.

Overall for this example of running the code enter "python event_planner.py Input_files/input_10.txt"

