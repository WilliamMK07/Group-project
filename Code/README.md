# Group-project

To run the file this can be done in the terminal using the python command. Using the terminal enter "python", then the name of the the desired file to run e.g. "Code/event_planner.py", finally choose the desired text file to run using algorithms and make sure the correct file path is used. For example if the list of text files are stored in a file called "Input_Files" use file name "Lists/input_10.txt" for example.

Overall, to run this program in the terminal use "python Code/event_planner.py Input_Files/input_10.txt", replacing the "input_10.txt" with a filem of your choice as well as the "event_planner.py" with a python file of your choice. Run this in the main file and not inside any enclosed files as some of the reasources may not be accessible

